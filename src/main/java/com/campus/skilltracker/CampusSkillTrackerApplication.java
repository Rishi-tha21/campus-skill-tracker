package com.campus.skilltracker;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main entry point for Campus Skill Tracker application.
 * A Spring Boot MVC application demonstrating One-to-Many relationships,
 * CRUD operations, custom JPQL queries, and JSP-based views.
 */
@SpringBootApplication
public class CampusSkillTrackerApplication {

    public static void main(String[] args) {
        SpringApplication.run(CampusSkillTrackerApplication.class, args);
    }
}
